// C++: Supports structures (struct) and unions (union) for grouping data.
//Java: Does not have struct or union, as it relies on class-based OOP.

struct Student {
    string name;
    int age;
};

union Data {
    int intVal;
    float floatVal;
};
